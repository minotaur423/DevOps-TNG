# Practice with tuples.
lists = [100,200,300]  # Main differences between lists and tuples is that 
tuples = (100,200,300) # lists can be edited, tuples cannot.

lists.append(400)
# tuples.append(400) is immutable, cannot be appended.

print(lists)
print(tuples)
